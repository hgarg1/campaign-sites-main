{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @campaignsites/web exec prisma generate && pnpm --filter @campaignsites/web build",
  "outputDirectory": "apps/web/.next"
}
